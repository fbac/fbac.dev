---
---

<section class="about">
  <h2><span class="prefix">// </span>About</h2>
  <p>15+ years building production systems — from Linux kernel and eBPF at Red Hat to decentralized protocol architectures at XMTP, ZetaChain, and Chainlink. I architect across layers: consensus-critical backends, on-chain settlement contracts, cross-chain indexers, and the infrastructure that runs it all.</p>
  <p>AI-augmented workflow — I use AI tooling to multiply output, regularly taking projects from zero to production.</p>
</section>

<style>
  section.about {
    padding: var(--section-gap) 0;
  }

  h2 {
    font-size: 1.4rem;
    margin-bottom: 1.5rem;
  }

  .prefix {
    color: var(--color-accent);
    opacity: 0.7;
  }

  p {
    margin-bottom: 1rem;
    color: var(--color-text-muted);
  }

</style>
